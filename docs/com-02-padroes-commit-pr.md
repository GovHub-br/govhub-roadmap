# Padrões de commit / branch / PR / testes

> [!TODO] Esqueleto — extrair do `CONTRIBUTING.md` atual e transformar em referência consultável, em vez de texto corrido.

Referência de consulta rápida das convenções do projeto. Para o passo a passo de como abrir uma contribuição, veja [Primeiro PR, passo a passo](#doc/com-09-primeiro-pr).

## Commits

*A escrever: formato adotado (Conventional Commits?), tipos aceitos, quando usar cada um.*

| Tipo | Usar quando |
| --- | --- |
| `feat` | *a escrever* |
| `fix` | *a escrever* |
| `docs` | *a escrever* |
| `refactor` | *a escrever* |
| `test` | *a escrever* |
| `chore` | *a escrever* |

*A escrever: assinatura de commit é obrigatória? A view Instalação já menciona configurar assinatura — confirmar e alinhar.*

## Branches

| Padrão | Uso |
| --- | --- |
| *a definir* | |

## Pull requests

*A escrever: título, descrição mínima, o que precisa estar verde antes de pedir revisão, quantos aprovadores.*

## Testes

*A escrever: o que precisa de teste, como rodar (`make test`), cobertura esperada, testes dbt.*

## Ver também

- Revisão de PRs — `gov-hub.io/documentacao/pipeline/protocolo-mr/`
- Git Workflow — `gov-hub.io/documentacao/onboarding/git-workflow/`
