# Como o GovHub roda em Kubernetes

> [!TODO] **Não existe hoje.** Documento proposto a partir de uma faixa vazia do roadmap: o desenho do cluster e por que cada serviço vive onde vive

*A escrever.*
