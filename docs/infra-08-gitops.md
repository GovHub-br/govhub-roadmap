# GitOps: como uma mudança chega em produção

> [!TODO] **Não existe hoje.** Documento proposto a partir de uma faixa vazia do roadmap: do commit ao Argo CD sincronizando, e por que não se aplica nada na mão

*A escrever.*
